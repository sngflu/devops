apiVersion: v1
kind: Service
metadata:
  name: minio
  namespace: lab4-app
spec:
  selector:
    app: minio
  ports:
    - port: 9000
      targetPort: 9000
      nodePort: 40900
      name: api
    - port: 9001
      targetPort: 9001
      nodePort: 40901
      name: console
  type: NodePort
