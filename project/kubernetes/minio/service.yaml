apiVersion: v1
kind: Service
metadata:
  name: minio
  namespace: lab4-app
spec:
  selector:
    app: minio
  ports:
    - port: 9000
      targetPort: 9000
      nodePort: 32900
      name: api
    - port: 9001
      targetPort: 9001
      nodePort: 32901
      name: console
  type: NodePort
