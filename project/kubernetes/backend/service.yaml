apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: lab4-app
  labels:
    app: backend
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - name: http
      port: 5174
      targetPort: 5174
      protocol: TCP
