apiVersion: v1
kind: Service
metadata:
  name: prometheus
  labels:
    app: prometheus
spec:
  ports:
  - port: 9090
    targetPort: web
    protocol: TCP
    name: http
  selector:
    app: prometheus
  type: ClusterIP 