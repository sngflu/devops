apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: lab4-app
  labels:
    app: prometheus
spec:
  type: NodePort
  selector:
    app: prometheus
  ports:
    - port: 9090
      targetPort: web
      nodePort: 31903
      protocol: TCP
      name: http
