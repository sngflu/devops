apiVersion: v1
kind: PersistentVolume
metadata:
  name: prometheus-pv-lab4
spec:
  capacity:
    storage: 3Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  persistentVolumeReclaimPolicy: Delete
  hostPath:
    path: /mnt/k8s/prometheus-pv-lab4
